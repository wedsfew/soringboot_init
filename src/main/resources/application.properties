spring.application.name=demo

# 数据库连接配置
spring.datasource.url=jdbc:mysql://dbprovider.ap-southeast-1.clawcloudrun.com:35964?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=j75fljbf
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 数据库初始化配置
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:db-init.sql,classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=true
spring.sql.init.separator=;

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.demo.entity
mybatis.configuration.map-underscore-to-camel-case=true

# 服务器配置
server.port=8080

# 日志配置
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=INFO
logging.level.org.mybatis=DEBUG

# 允许循环引用
spring.main.allow-circular-references=true